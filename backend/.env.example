# Copy this file to .env and fill in your values
PORT=5000

# MongoDB connection string (optional — server runs without it using in-memory store)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/workshop?retryWrites=true&w=majority

# Allowed frontend origin (use * for development)
CLIENT_URL=http://localhost:5173
